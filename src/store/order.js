const state = {
  type: 'order'
}

const mutations = {

}

const actions = {

}

const getters = {
  Types: (state) => {
    return state.type
  }
}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}