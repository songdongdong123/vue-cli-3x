const state = {
  type: 'user'
}

const mutations = {

}

const actions = {

}

const getters = {
  Type: (state) => {
    return state.type
  }
}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}